# first put a name to workflow
name: First Workflow
# When is going to execute the workflow, the trigger
on: workflow_dispatch
jobs:
  first-job:
    runs-on: macos-latest
    steps: 
      - name: Print greeting
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Done - bye!"
        
